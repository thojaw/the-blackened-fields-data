{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/thojaw/the-blackened-fields-data/schema/artists.schema.json",
  "title": "ArtistRegistry",
  "description": "Canonical schema for the repo-root artists.json global artist registry, matching AGENTS.md exactly.",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": ["id", "name"],
    "properties": {
      "id": {
        "type": "string",
        "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
        "description": "Permanent kebab-case slug, derived from name (diacritics stripped, lowercased). Never changes once assigned. Referenced by festival.json's Artist.globalId (or directly as Artist.id for a new single-show artist)."
      },
      "name": {
        "type": "string",
        "description": "Display name, may include diacritics, e.g. 'Drekka Sjór'."
      },
      "description": {
        "type": "string",
        "description": "Free-text bio, same style as festival.json's Artist.description."
      },
      "genres": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Free-text genre labels, same convention as festival.json's Artist.genres."
      },
      "country": {
        "type": "string",
        "pattern": "^[a-z]{2}$",
        "description": "ISO 3166-1 alpha-2 country code, lowercase, e.g. 'us', 'de'. Omitted rather than guessed when unresolved."
      },
      "links": {
        "type": "array",
        "items": { "$ref": "#/$defs/RegistryLink" },
        "description": "Artist's own persistent links (official site, socials, streaming), independent of any single festival's links[]. Same rendering rules as festival.json's ExternalLink, minus the id/artistId fields which have no meaning outside a festival's links array."
      }
    }
  },
  "$defs": {
    "RegistryLink": {
      "type": "object",
      "additionalProperties": false,
      "required": ["url"],
      "properties": {
        "label": {
          "type": "string",
          "description": "Required when type is absent (text link); optional when type is present (a11y/tooltip only)."
        },
        "url": { "type": "string", "format": "uri-reference" },
        "type": {
          "type": "string",
          "enum": [
            "web", "facebook", "x", "youtube", "instagram", "spotify",
            "deezer", "bandcamp", "applemusic", "soundcloud", "tiktok",
            "patreon", "discord"
          ],
          "description": "Absent = text link. Present = icon-only rendering."
        }
      },
      "if": {
        "not": { "required": ["type"] }
      },
      "then": {
        "required": ["label"]
      }
    }
  }
}
